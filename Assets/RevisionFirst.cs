using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class RevisionFirst : MonoBehaviour {

	public GameObject RevisionObject;

	// Use this for initialization
	void Start () {

		RevisionObject.SetActive (false);
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
